require "spec_helper"

RSpec.describe Metadatajson2puppetfile do
  it "has a version number" do
    expect(Metadatajson2puppetfile::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
